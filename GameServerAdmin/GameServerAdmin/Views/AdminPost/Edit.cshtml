@model GameServerAdmin.Models.Posts.AdminUi.AdminPostEditViewModel

@{
    ViewData["Title"] = "게시글 수정";
}

<div class="mb-2">
    <a asp-controller="AdminUiPost" asp-action="Detail" asp-route-id="@Model.PostId"
       class="text-muted small">&larr; 상세로</a>
</div>

<div class="admin-panel" style="max-width:800px;">
    <div class="admin-panel-header">
        <h6>게시글 수정 #@Model.PostId</h6>
    </div>
    <div class="admin-panel-body">
        <form asp-action="Edit" asp-controller="AdminUiPost" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="PostId" />

            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3">
                <label asp-for="Title" class="form-label small fw-bold"></label>
                <input asp-for="Title" class="form-control form-control-sm" />
                <span asp-validation-for="Title" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Content" class="form-label small fw-bold"></label>
                <textarea asp-for="Content" class="form-control form-control-sm" rows="15"></textarea>
                <span asp-validation-for="Content" class="text-danger small"></span>
            </div>

            <div class="d-flex justify-content-end gap-2">
                <a asp-controller="AdminUiPost" asp-action="Detail" asp-route-id="@Model.PostId"
                   class="btn btn-outline-secondary btn-sm">취소</a>
                <button type="submit" class="btn btn-primary btn-sm">저장</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
